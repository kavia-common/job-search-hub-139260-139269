# job-search-hub-139260-139269

This workspace contains the Ocean Jobs frontend (React) at `job_platform_frontend/`.

Run:
- cd job_platform_frontend
- npm install
- npm start

The app is a no-auth job search UI using Remotive API by default and supports optional JSearch via RapidAPI with an .env key.